// src/full_adder.veryl

module FullAdder (
    a    : input  logic,
    b    : input  logic,
    c    : input  logic,
    sum  : output logic,
    carry: output logic,
) {
    assign sum   = a ^ b ^ c;
    assign carry = (a & b) | (a & c) | (b & c);
}
